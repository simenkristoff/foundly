/**
 * Configuration file for PostCSS-plugin.
 * This plugin is set up to autoprefix the app's stylesheet
 * in order to display correct style for all browsers.
 *
 */

module.exports = {
  plugins: {
    autoprefixer: {},
  },
};
